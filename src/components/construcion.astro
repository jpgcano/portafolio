<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página en Construcción</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div class="construction-container">
        <h1 class="title">¡Página en Construcción!</h1>
        <p class="subtitle">Estamos trabajando para traerte algo increíble. ¡Vuelve pronto!</p>
        <div class="loader"></div>
    </div>
</body>
</html>
<style>
    /* Reset básico */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body, html {
    height: 100%;
    font-family: Arial, sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    background: #f4f4f9;
    color: #333;
    text-align: center;
    overflow: hidden;
}

/* Contenedor principal */
.construction-container {
    max-width: 500px;
    padding: 20px;
    border: 2px solid #e0e0e0;
    border-radius: 8px;
    background-color: #fff;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

/* Título */
.title {
    font-size: 2.5em;
    color: #ff6347;
    margin-bottom: 10px;
    animation: pulse 1.5s infinite;
}

/* Subtítulo */
.subtitle {
    font-size: 1.2em;
    color: #555;
    margin-bottom: 20px;
}

/* Animación de latido para el título */
@keyframes pulse {
    0%, 100% {
        transform: scale(1);
    }
    50% {
        transform: scale(1.05);
    }
}

/* Loader circular */
.loader {
    border: 8px solid #e0e0e0;
    border-top: 8px solid #ff6347;
    border-radius: 50%;
    width: 50px;
    height: 50px;
    animation: spin 1s linear infinite;
    margin: 0 auto;
}

/* Animación del spinner */
@keyframes spin {
    0% {
        transform: rotate(0deg);
    }
    100% {
        transform: rotate(360deg);
    }
}

</style>